<%= form_with model: breed, local:true do |form| %>

<% if @breed.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@breed.errors.count, "error") %> prohibited this form from being saved:</h2>
 
    <ul>
    <% @breed.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="breed-form">
  <%= form.label :name %>
  <%= form.text_field :name, :required => true, :autofocus => true, :placeholder => "Type something here", :pattern => "[^0-9]*" %>
</div>

<br>
<div>
  <%= form.submit %>
</div>

<% end %>

<br>
